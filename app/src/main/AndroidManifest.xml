<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.game.snda.gameassistant" >

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Game.App" >

        <meta-data android:name="APP_CHANNEL" android:value="GAME_ASSISTANT"/>

        <activity
            android:name=".GuideActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SplashScreenActivity_"
            android:screenOrientation="portrait">
        </activity>

    </application>

</manifest>
